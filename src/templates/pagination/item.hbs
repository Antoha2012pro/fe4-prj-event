{{#if isDots}}
<span class="pagination__dot">...</span>
{{else}}
<button class="pagination__btn {{#if isActive}}pagination__btn--active{{/if}}" type="button" data-page="{{page}}" aria-label="Select a page">
  {{num}}
</button>
{{/if}}